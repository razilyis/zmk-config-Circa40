# 左右共通の設定（ZMK は circa40_left / circa40_right の両方に対して
# この circa40.conf を読み込む）。
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# NOTE: CONFIG_ZMK_POINTING はここには置かない。
# トラックボールは Right(central) にしか無いため、
# circa40_right.conf 側でのみ有効化する。
