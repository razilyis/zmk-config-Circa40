# Circa40 Left = Split の peripheral 側。
# トラックボール等のポインティングハードウェアは Right(central) 側にのみ
# 搭載されているため、この側で有効化すべきものは現状ない。

# ポインティングは Right(central) 専用。
# peripheral 側で有効にしてもマウス HID のコードが積まれるだけで使われない。
CONFIG_ZMK_POINTING=n
